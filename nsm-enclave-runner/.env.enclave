# Tell the runner we’re in AWS Nitro
RUNNER_ATTESTOR=aws

# Bind over vsock, not TCP
RUNNER_LISTENER=vsock

# Only the PORT part is used when listener=vsock
RUNNER_PUBLIC_ADDR=127.0.0.1:8443

# Optional “host” channel over vsock with mTLS
# Provide a CA PEM inside the enclave image if you use this
# RUNNER_HOST_CA_PEM_PATH=/app/host-ca.pem
# RUNNER_HOST_ADDR=127.0.0.1:9443

# Ratls options (you can usually leave these empty, clients pin the cert)
# RUNNER_RATLS_SAN_DNS=
# RUNNER_RATLS_SAN_IPS=
# RUNNER_RATLS_MIN_NONCE_LEN=32
# RUNNER_RATLS_QUOTE_OID=1.3.6.1.4.1.55555.1

# Logging
RUST_LOG=info
